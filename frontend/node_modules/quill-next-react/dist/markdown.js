var a = Object.defineProperty;
var p = (c, i, e) => i in c ? a(c, i, { enumerable: !0, configurable: !0, writable: !0, value: e }) : c[i] = e;
var h = (c, i, e) => p(c, typeof i != "symbol" ? i + "" : i, e);
import { useEffect as g } from "react";
import { u as k } from "./use-quill-UZ1S5QeU.js";
import { marked as L } from "marked";
function v({
  value: c
}) {
  const i = k();
  return g(() => {
    if (!c)
      return;
    const e = L.parse(c), t = i.clipboard.convert({
      html: e,
      text: `
`
    }), n = i.getContents().diff(t);
    i.updateContents(n);
  }, [c]), null;
}
class y {
  constructor() {
    h(this, "outputLines");
    h(this, "currentLineSegments");
    h(this, "activeBlockAttributes");
    h(this, "orderedListCounters");
    // Keyed by indent level
    h(this, "inCodeBlock");
    h(this, "codeBlockLanguage");
    this.outputLines = [], this.currentLineSegments = [], this.activeBlockAttributes = {}, this.orderedListCounters = {}, this.inCodeBlock = !1, this.codeBlockLanguage = "";
  }
  resetLineBuffers() {
    this.currentLineSegments = [];
  }
  initializeConversionState() {
    this.outputLines = [], this.currentLineSegments = [], this.activeBlockAttributes = {}, this.orderedListCounters = {}, this.inCodeBlock = !1, this.codeBlockLanguage = "";
  }
  _applyInlineFormatting(i, e = {}) {
    let t = i;
    const s = [];
    if (e.color && s.push(`color: ${e.color};`), e.background && s.push(`background-color: ${e.background};`), e.font && s.push(`font-family: ${e.font};`), e.size) {
      const n = {
        small: "0.8em",
        large: "1.2em",
        huge: "1.5em"
      };
      s.push(`font-size: ${n[e.size] || e.size};`);
    }
    return s.length > 0 && (t = `<span style="${s.join(" ")}">${t}</span>`), e.underline && (t = `<u>${t}</u>`), e.strike && (t = `<s>${t}</s>`), e.script === "sub" && (t = `<sub>${t}</sub>`), e.script === "super" && (t = `<sup>${t}</sup>`), e["code-block"] === void 0 && e.code && (t = `\`${t}\``), e.link && (t.includes("<") || t.includes(">") ? t = `<a href="${e.link}">${t}</a>` : t = `[${t}](${e.link})`), e.italic && (t = `*${t}*`), e.bold && (t = `**${t}**`), t;
  }
  _renderCurrentLine() {
    if (!this.currentLineSegments.length && !this.activeBlockAttributes.list) {
      this.inCodeBlock && this.outputLines.push("");
      return;
    }
    const i = this.currentLineSegments.map((o) => this._applyInlineFormatting(o.text, o.attributes)).join("");
    if (this.resetLineBuffers(), this.inCodeBlock) {
      this.outputLines.push(i);
      return;
    }
    let e = "";
    const t = "";
    let s = null;
    const n = this.activeBlockAttributes, r = [];
    if (n.align && n.align !== "left" && r.push(`text-align: ${n.align};`), n.indent && n.indent > 0 && !n.list && r.push(`padding-left: ${n.indent * 2}em;`), n.list) {
      const o = n.indent || 0, l = "  ".repeat(o);
      switch (n.list) {
        case "ordered":
          this.orderedListCounters[o] = (this.orderedListCounters[o] || 0) + 1, e = `${l}${this.orderedListCounters[o]}. `;
          break;
        case "bullet":
          e = `${l}* `;
          break;
        case "checked":
          e = `${l}- [x] `;
          break;
        case "unchecked":
          e = `${l}- [ ] `;
          break;
      }
    } else n.blockquote ? (e = "> ", this.orderedListCounters = {}) : n.header ? (r.length > 0 ? (s = {
      open: `<h${n.header} style="${r.join(" ")}">`,
      close: `</h${n.header}>`
    }, r.length = 0) : e = "#".repeat(n.header) + " ", this.orderedListCounters = {}) : this.orderedListCounters = {};
    r.length > 0 && !s && (s = {
      open: `<div style="${r.join(" ")}">`,
      close: "</div>"
    }), s ? this.outputLines.push(`${s.open}${e}${i}${t}${s.close}`) : this.outputLines.push(`${e}${i}${t}`);
  }
  _processEmbed(i, e = {}) {
    if (this.currentLineSegments.length > 0 && this._renderCurrentLine(), this.activeBlockAttributes = {}, i.image) {
      const t = i.image, s = e.alt || "Image", n = e.width, r = e.height, o = e.align;
      let l = "";
      if (o === "center" ? l = "display: block; margin-left: auto; margin-right: auto;" : o === "right" ? l = "float: right; margin-left: 1em;" : o === "left" && (l = "float: left; margin-right: 1em;"), n || r || l) {
        const u = n ? `width="${n}"` : "", d = r ? `height="${r}"` : "", f = l ? `style="${l}"` : "";
        this.outputLines.push(`<img src="${t}" alt="${s}" ${u} ${d} ${f}>`), l.includes("float:") && this.outputLines.push('<br style="clear: both;" />');
      } else
        this.outputLines.push(`![${s}](${t})`);
    } else if (i.video) {
      const t = i.video, s = e.width || "560", n = e.height || "315";
      this.outputLines.push(`<iframe src="${t}" width="${s}" height="${n}" frameborder="0" allowfullscreen></iframe>`);
    } else
      this.outputLines.push("");
    this.activeBlockAttributes = {}, this.orderedListCounters = {};
  }
  _processNewline(i) {
    if (this.inCodeBlock && !i["code-block"] && (this.outputLines.push("```"), this.inCodeBlock = !1, this.codeBlockLanguage = ""), this.activeBlockAttributes = {
      ...i
    }, !this.activeBlockAttributes.list)
      this.orderedListCounters = {};
    else {
      const e = i.indent || 0;
      Object.keys(this.orderedListCounters).forEach((t) => {
        const s = parseInt(t, 10);
        s > e && delete this.orderedListCounters[s];
      });
    }
    this._renderCurrentLine(), !this.inCodeBlock && this.activeBlockAttributes["code-block"] && (this.inCodeBlock = !0, this.codeBlockLanguage = typeof this.activeBlockAttributes["code-block"] == "string" ? this.activeBlockAttributes["code-block"] : "", this.outputLines.push(`\`\`\`${this.codeBlockLanguage}`));
  }
  convert(i) {
    this.initializeConversionState();
    for (const t of i) {
      if (t.insert === void 0 || t.insert === null) continue;
      const s = t.attributes || {};
      if (typeof t.insert == "string") {
        const r = t.insert.split(`
`);
        for (let o = 0; o < r.length; o++) {
          const l = r[o];
          l && this.currentLineSegments.push({
            text: l,
            attributes: s
          }), o < r.length - 1 && this._processNewline(s);
        }
      } else typeof t.insert == "object" && this._processEmbed(t.insert, s);
    }
    return this.currentLineSegments.length > 0 && this._processNewline(this.activeBlockAttributes), this.inCodeBlock && (this.outputLines.push("```"), this.inCodeBlock = !1), this.outputLines.join(`

`).trim();
  }
}
export {
  y as DeltaToMarkdown,
  v as MarkdownPlugin
};
