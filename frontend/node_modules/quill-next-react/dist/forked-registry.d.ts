import { Registry, RegistryDefinition, Scope } from "parchment";
export declare class ForkedRegistry extends Registry {
    #private;
    parent: Registry;
    constructor(parent: Registry);
    register(...definitions: RegistryDefinition[]): RegistryDefinition[];
    query(query: string | Node | Scope, scope?: Scope): RegistryDefinition | null;
}
